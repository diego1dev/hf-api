import { injectable } from 'inversify';
import { IConsumptionService } from '../interfaces/IConsumptionService';

@injectable()
export class ConsumptionService implements IConsumptionService {
}
export default ConsumptionService;
